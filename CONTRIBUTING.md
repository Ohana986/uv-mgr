# 贡献指南

欢迎提交问题报告和改进建议。提交代码前请确保：

1. 运行现有测试并保持全部通过。
2. 运行时继续保持仅使用 Python 标准库。
3. 为行为变更补充测试和更新文档。
4. 提交信息清楚描述变更目的。

依赖同步和测试环境准备由贡献者自行执行，项目维护者不会在发布流程中代为安装软件包。

构建发行物：

```bash
uv-mgr sync
python -m build --sdist --wheel
```

如果项目根目录存在旧的 `build/` 构建残留目录，请先手动删除该目录，再运行构建命令；否则它可能遮蔽 Python 的 `build` 构建工具包。
